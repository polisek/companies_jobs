RegisterNetEvent("companies:server:YOUREVENTNAME")
AddEventHandler("companies:server:YOUREVENTNAME", function(count)
    local src = source
    if CanTrustPlayer(src) then
        
    end
end)
